<h1 class="title">
	<%= @post.name %> is...
</h1>

<p class="secret">
	<%= @post.secret %>
</p>

<p class="date">
	Submitted <%= time_ago_in_words(@post.created_at) %> Ago
</p>

<%= link_to 'Index', root_path(@post) %> | <%= link_to 'Edit', edit_post_path(@post) %> |
		  <%= link_to 'Delete', post_path(@post), method: :delete, data: { confirm: 'Are you sure?' } %>
	</p>
